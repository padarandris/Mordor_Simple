@model IEnumerable<Mordor_Simple.Models.Horde>

@{
    ViewBag.Title = "Hordes";
}

<h2>Horde list</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.HordeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.HordeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.HordeId })
        </td>
    </tr>
}

</table>
